const initialState = {
  conversations: [],
};

const reducer = (state = initialState, action) => {
  switch (action.type) {
    default:
      console.log("reducer is setup");
      break;
  }
};

export default reducer;
